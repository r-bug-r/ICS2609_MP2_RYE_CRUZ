build.classes.dir=${build.dir}/web/WEB-INF/classes
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
build.web.dir=${build.dir}/web
conf.dir=${src.dir}/conf
dist.dir=dist
dist.javadoc.dir=${dist.dir}/javadoc
dist.war=${dist.dir}/ICS2609_2A_RYE_GROUP.war
includes=**
javac.deprecation=false
javac.source=1.8
javac.target=1.8
javac.test.classpath=${javac.classpath}:${build.classes.dir}
j2ee.platform=1.7
j2ee.server.type=GlassFish
lib.dir=lib
src.dir=src/java
war.content.additional=
war.name=ICS2609_2A_RYE_GROUP.war
web.docbase.dir=web
webinf.dir=web/WEB-INF
build.classes.excludes=**/*.java,**/*.form
client.urlPart=
debug.classpath=${javac.classpath}:${build.classes.dir}
debug.test.classpath=${javac.test.classpath}:${build.classes.dir}
display.browser=true
java.api.stub.dir=${build.dir}/api-stubs
javac.classpath=
javac.test.classpath=
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
resource.inclusion.types=hi
source.encoding=UTF-8
