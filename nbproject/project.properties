build.classes.dir=${build.dir}/web/WEB-INF/classes
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
build.web.dir=${build.dir}/web
conf.dir=${src.dir}/conf
dist.dir=dist
dist.war=${dist.dir}/ICS2609_2A_RYE_GROUP.war
includes=**
javac.deprecation=false
javac.source=1.8
javac.target=1.8
javac.test.classpath=${javac.classpath}:${build.classes.dir}
j2ee.platform=1.7
j2ee.server.type=GlassFish
lib.dir=lib
src.dir=src/java
war.content.additional=
war.name=ICS2609_2A_RYE_GROUP.war
web.docbase.dir=web
webinf.dir=web/WEB-INF
